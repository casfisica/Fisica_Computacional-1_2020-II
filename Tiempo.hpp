/**
 *Declaración de la estructura Tiempo
 *Declaración de la función PrintTiempo
 */

#ifndef TIEMPO_H 
#define TIEMPO_H

struct Tiempo {
  int hora;
  int minuto;
};

void PrintTiempo(Tiempo t);

#endif
